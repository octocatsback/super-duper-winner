# Copy to .env.local (gitignored). Never put real values in tracked files.
# Create these as Cursor environment secrets or GitHub Actions secrets instead.

# Fine-grained PAT: Copilot Requests = Read
COPILOT_GITHUB_TOKEN=

# Fine-grained PAT for GitGoat against a disposable test org only
GITGOAT_GITHUB_TOKEN=

# Copy to .env.local (gitignored). Never put real values in tracked files.
COPILOT_GITHUB_TOKEN=
GITGOAT_GITHUB_TOKEN=
# Starlink Public API v2 — Business service account only. Leave empty on consumer kits.
STARLINK_CLIENT_ID=
STARLINK_CLIENT_SECRET=
